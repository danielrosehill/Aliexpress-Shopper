---
name: aliexpress-combos
description: Verify, query, or toggle the Hide Combo Deals userscript on the active AliExpress tab. Hands off to the `hide-combo-deals` skill.
---

# /aliexpress-combos

Check on or toggle the **Hide Combo Deals** userscript on the active AliExpress tab.

Usage:
- `/aliexpress-combos` — report state (active? how many excluded?).
- `/aliexpress-combos toggle` — flip Show/Hide.
- `/aliexpress-combos hide` — ensure combos are hidden (no-op if already).
- `/aliexpress-combos show` — ensure combos are visible (no-op if already).
